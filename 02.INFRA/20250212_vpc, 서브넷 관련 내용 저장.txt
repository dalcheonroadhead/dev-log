IAM - 사용자 그룹 별로 권한 줄 수 있음. 사용자 단일 권한을 줄 수 있음. 

팀원에게 사용자 계정을 줘서 AWS 쓰게 하기 
IAM 접속 - 사용자 - 사용자 생성 - 기존 사용자 그룹에 등록 - 사용자 생성 - 생성한 사용자 들어가기 
- 보안 자격 증명 - 콘솔 엑세스 활성화 - 자동 생성화된 암호 - 사용자는 다음 로그인 시 새로운 암호를 생성해야 합니다. -> 콘솔 로그인 URL, 사용자 이름, 콘솔 암호 알려주기 

vpc - 네트워크 구성 

사설IP (IPv4) 대역 잡아주고 VPC 생성 
해당 VPC를 EC2에 할당 (VPC: 경기장, 서브넷: 통로) 
VPC를 서브넷으로 짜르기

서브넷 마스크  - 앞 단 2진수를 가려준다. 

public - 외부랑 쌍방 통신 가능
private - 외부랑 연결 불가 내부랑만 쓰임 
NAT GATEWAY - private가 외부로부터 정보를 가져올 수 있는 통로 
 
public 서브넷 - 인터넷 게이트 웨이랑 연결 (인터넷 게이트 웨이 생성) 
서브넷 - 인터넷 게이트 웨이 연결 -> 라우팅 사용 


서브넷 -> 라우팅 테이블 -> 인터넷 게이트 웨이  
private 도 라우팅 테이블 연결 
why? 다른 서브넷과의 통신을 위해 


EC2 만들기 

탄력적 IP 하기 -> 안하면 EC2 온오프마다 주소가 바뀜
